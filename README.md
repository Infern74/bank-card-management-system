# 🏦 Система управления банковскими картами

## 📖  Описание
REST API для полного цикла управления банковскими картами с расширенной системой безопасности,
ролевой моделью доступа и полным аудитом операций.
Система поддерживает создание карт, управление ими, переводы между картами и систему запросов на блокировку.

## Технологии
- Java 17
- Spring Boot 3.2.3
- Spring Security
- Spring Data JPA
- PostgreSQL
- Liquibase
- JWT
- Docker Compose
- OpenAPI 3 (Swagger)

## Требования
- Java 17+
- Maven 3.6+
- Docker & Docker Compose

## 🚀 Запуск приложения

### Способ 1: Docker Compose (рекомендуется)
```bash
# Собрать приложение
mvn clean package

# Запустить все сервисы
docker-compose up -d
```

### Способ 2: Локальный запуск
```bash
# Запустить PostgreSQL
docker run --name bank-postgres -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=bank_db -p 5432:5432 -d postgres:15-alpine

# Собрать и запустить приложение
mvn clean package
java -jar target/bank-card-system-1.0.0.jar
```

## 🔗 Доступные сервисы
- Приложение: http://localhost:8080

- Swagger UI: http://localhost:8080/swagger-ui.html

- База данных: localhost:5432

- API Документация: http://localhost:8080/api-docs

## 👥 Демо пользователи
- Администратор: admin / admin123

- Пользователь: user1 / user123

## 🔐 Аутентификация и Авторизация
Система использует JWT (JSON Web Tokens) для аутентификации. После успешного входа через ```/auth/login``` вы получите
токен, который нужно включать в заголовок всех последующих запросов:

```http
Authorization: Bearer <ваш_jwt_токен>
```
### Ролевая модель:
ADMIN - Полный доступ ко всем операциям

USER - Доступ только к своим данным и операциям

## 📚 API Endpoints
### 🔑 Аутентификация

- POST /auth/login - Аутентификация пользователя и получение JWT токена

- POST /auth/register - Регистрация нового пользователя (роль USER)

### 💳 Карты (требуют аутентификации)
#### Для администратора:

- GET /cards - Получение всех карт в системе

- POST /cards - Создание новой карты для пользователя

- GET /cards/{id} -	Получение любой карты по ID

- PUT /cards/{id}/block -	Принудительная блокировка карты

- PUT /cards/{id}/activate - Активация заблокированной карты

- DELETE /cards/{id} - Удаление карты (только с нулевым балансом)

#### Для пользователя:

- GET /cards/my - Получение своих карт

- GET /cards/{id} - Получение своей карты по ID

- GET /cards/{id}/balance - Просмотр баланса своей карты

- GET /cards/my/search - Поиск своих карт (по ID, имени держателя, последним 4 цифрам)

- GET /cards/my/filter - Фильтрация карт по статусу (ACTIVE, BLOCKED, EXPIRED)

### 📝 Запросы на блокировку карт (/card-block-requests/**)
#### Для пользователя:

- POST /card-block-requests - Создание запроса на блокировку своей карты

- GET /card-block-requests/my - Получение своих запросов на блокировку

- POST /card-block-requests/{requestId}/cancel - Отмена своего запроса на блокировку

#### Для администратора:

- GET /card-block-requests - Получение всех запросов на блокировку

- GET /card-block-requests/status/{status} - Фильтрация запросов по статусу (PENDING, APPROVED, REJECTED, CANCELLED)

- POST /card-block-requests/{requestId}/approve - Одобрение запроса на блокировку

- POST /card-block-requests/{requestId}/reject - Отклонение запроса на блокировку

### 💰 Переводы
#### Для пользователя:
- POST /transfers - Создание перевода между своими картами

- GET /transfers/my - Получение истории своих переводов

- GET /transfers/{id} - Получение деталей конкретного перевода

- PUT /transfers/{id}/cancel - Отмена своего перевода (в течение 24 часов)

### 👥 Управление пользователями (/admin/users/**)
#### Только для администратора:

- GET /admin/users - Получение списка всех пользователей

- POST /admin/users/{userId}/block - Блокировка пользователя

- POST /admin/users/{userId}/activate -	Активация пользователя

### 🩺 Система

- GET /health - Проверка работоспособности сервиса

## 🎯 Функциональность
### Для администратора:

- ✅ Полное управление картами: создание, блокировка, активация, удаление

- ✅ Управление пользователями: просмотр, блокировка, активация

- ✅ Просмотр всех данных: все карты, все переводы, все запросы

- ✅ Одобрение/отклонение запросов: управление системой запросов на блокировку

- ✅ Аудит операций: полный доступ ко всем операциям в системе

### Для пользователя:

- ✅ Просмотр своих карт: с поддержкой пагинации, поиска и фильтрации

- ✅ Запрос блокировки карт: система запросов с указанием причины

- ✅ Переводы между картами: безопасные переводы между своими картами

- ✅ Отслеживание операций: просмотр истории переводов и запросов

- ✅ Отмена операций: возможность отмены переводов (в течение 24 часов)

## 🔒 Безопасность
### Шифрование данных:
- AES-256 шифрование: номера карт и CVV хранятся в зашифрованном виде

- SHA-256 хэширование: для поиска карт используется хэш номера карты

- Маскирование данных: номера карт отображаются в формате **** **** **** 1234

### Защита доступа:
- JWT аутентификация: токены с подписью HMAC SHA-256

- Ролевой контроль доступа: разделение на ADMIN/USER

- Проверка владения: пользователи имеют доступ только к своим данным

- BCrypt хэширование: пароли хранятся в безопасном виде

### Валидация:
- ✅ Проверка формата номера карты (16 цифр)

- ✅ Проверка срока действия карты (не менее 1 месяца)

- ✅ Валидация CVV (3-4 цифры)

- ✅ Проверка баланса при переводах

- ✅ Лимиты на суммы операций

## 🗄️ База данных
### Структура базы данных:

```sql
users (пользователи)
├── user_roles (роли пользователей)
├── cards (банковские карты)
│   ├── transfers (переводы между картами)
│   └── card_block_requests (запросы на блокировку)
```

### Миграции:
- Liquibase: автоматическое применение миграций при запуске

- История изменений: полный трекинг изменений схемы БД

- Предустановленные данные: администратор и тестовый пользователь

### Индексы:
- Оптимизированные индексы для часто используемых запросов

- Индексы по статусам карт и запросов

- Индексы для поиска по пользователям

## ⚙️ Конфигурация
### Настройки приложения (application.yml):
```yaml
app:
jwt:
secret: ваш_секретный_ключ # для подписи JWT
expiration: 86400000 # срок жизни токена (24 часа)
encryption:
key: ключ_шифрования # для AES-256 шифрования
card:
mask-pattern: "**** **** **** %s" # формат маскирования
max-initial-balance: 1000000.00 # максимальный начальный баланс
transfer:
max-amount: 1000000.00 # максимальная сумма перевода
```
### Переменные окружения Docker:
- ```SPRING_DATASOURCE_URL```: URL базы данных

- ```SPRING_DATASOURCE_USERNAME```: пользователь БД

- ```SPRING_DATASOURCE_PASSWORD```: пароль БД

- ```APP_JWT_SECRET```: секрет для JWT

- ```APP_ENCRYPTION_KEY```: ключ шифрования


## 🧪 Тестирование
```bash
# Запуск тестов
mvn test

# Запуск с покрытием
mvn jacoco:report
```

### Структура тестов:
- Юнит-тесты: бизнес-логика сервисов

- Интеграционные тесты: работа с БД и API

- Тесты безопасности: проверка прав доступа

## 📊 Особенности реализации
### Автоматическое управление статусами:
- Ежедневное обновление: автоматическая проверка просроченных карт

- Фоновая обработка: обновление статусов через scheduled tasks

- Консистентность: гарантия корректного статуса карт

### Система запросов на блокировку:
- Workflow: PENDING → APPROVED/REJECTED/CANCELLED

- Аудит: полное логирование всех изменений статусов

- Уведомления: система комментариев для администратора

### Обработка переводов:
- Транзакционность: гарантия атомарности операций

- Проверки: валидация перед выполнением перевода

- Отмена: возможность отмены в течение 24 часов

## 🚫 Ограничения и валидации
### Карты:
- ❌ Нельзя создать карту с истекшим сроком действия

- ❌ Нельзя заблокировать уже заблокированную карту

- ❌ Нельзя активировать просроченную карту

- ❌ Нельзя удалить карту с положительным балансом

- ❌ Нельзя создать дубликат номера карты

### Переводы:
- ❌ Максимальная сумма перевода: 1,000,000.00

- ❌ Отмена перевода возможна только в течение 24 часов

- ❌ Переводы только между картами одного пользователя

- ❌ Нельзя перевести на ту же карту

### Пользователи:
- ❌ Уникальные username и email

- ❌ Минимальная сложность пароля

- ❌ Только администратор может менять роли

## 📈 Мониторинг и документация
### Swagger/OpenAPI:

- Полная документация: все endpoints с описанием

- Интерактивное тестирование: возможность отправки запросов

- Схемы данных: подробное описание DTO моделей

- Авторизация: поддержка JWT в UI

### Health check:
- Проверка БД: доступность соединения с PostgreSQL

- Проверка сервиса: общий статус приложения

- Версия: информация о версии системы